# Docker Compose Environment Variables

# OpenClaw Configuration
OPENCLAW_IMAGE=moltbot:latest
OPENCLAW_GATEWAY_TOKEN=your-secure-token-here
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan

# Directories (sử dụng đường dẫn tuyệt đối hoặc tạo thư mục local)
OPENCLAW_CONFIG_DIR=./config
OPENCLAW_WORKSPACE_DIR=./workspace

# Claude API Keys (optional - để trống nếu không dùng)
CLAUDE_AI_SESSION_KEY=
CLAUDE_WEB_SESSION_KEY=
CLAUDE_WEB_COOKIE=

# Slack Configuration
# Lấy tokens từ: https://api.slack.com/apps
# 1. App Token (xapp-...): Socket Mode → Basic Information → App-Level Tokens
# 2. Bot Token (xoxb-...): OAuth & Permissions → Bot User OAuth Token
# 3. User Token (xoxp-..., optional): OAuth & Permissions → User OAuth Token
SLACK_APP_TOKEN=xapp-your-app-token-here
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_USER_TOKEN=

# Ollama Configuration
OLLAMA_BASE_URL=http://ollama:11434/v1
